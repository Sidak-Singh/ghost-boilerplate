{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install -g yarn@1.22.19 && yarn install --frozen-lockfile && yarn build",
    "startCommand": "yarn start"
  }
}
